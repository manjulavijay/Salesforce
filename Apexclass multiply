//Declares a List called “input”, which will be used to hold 4 numbers: “12, 20, 30, 17”
• Loop through “input”, for each number display “Ding” via System Debug if “input” is
multiples of 3 and “Dong” if it is multiples of 5
• If “input” is multiples of both 3 and 5, display “DingDong”
• For input that are neither multiples of 3 or 5, returns the input number.
• Below is an example of “input” and what gets displayed:
Input Display
12 (multiple of 3) Ding 20 (multiple of 5) Dong
30 (multiple of 3 and 5) DingDong 17 (not multiples of 3 or 5) 17

public class mission5MathMethod {
    public static void mathWork(){
        //new list called input created and it's value assigned
        List<Integer> input = new List<Integer>{12,20,30,17};
            //output assigned to Ding and Dong
            
            for (Integer i : input){
                String output1 = 'Ding';
                String output2 = 'Dong';
                //multiple of 3 and 5 with reminder as 0 to print dingdong
                if(math.mod(i,3) == 0 && math.mod(i,5) == 0){
                    System.debug(output1+output2);
                }
                 //only multiple of 3 with reminder as 0
                else if(math.mod(i,3) == 0){
                    System.debug(output1);
                }
                // only multiple of 5 with reminder as 0
                else if(math.mod(i,5) == 0){
                    System.debug(output2);
                }
                 // if not mutiply of 3 and 5 
                else{
                    system.debug(i);
                }
            } 
           
            
    }

}
